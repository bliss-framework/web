# Changelog

All notable changes to this project will be documented in this file.

The format is loosely based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), but because this is a docs site with no versioned releases, entries are grouped by date instead of by release version. Newest date on top. Within a date block, group by change type (`Added` / `Changed` / `Fixed` / `Removed`).

## [2026-09-12]

### Added

- **"Say what the value holds" naming rule**: added a new section to `coding-guidelines/general-naming-conventions.md` (after *Name structure*, before *Language specific conventions*). A name must convey the *value* a variable holds — codes vs ids vs full codes — not merely the concept it relates to. Worked examples: a `text[]` of permission full codes is `permission_full_codes`, not `perms`; an id is `user_id` while the row/object is `user`; an expiry date is `expiration_date`, not `expiration`; and a bare `color` is ambiguous (`"red"` / `"#ff0000"` / `"rgb(255,0,0)"` / palette id?) so it should be `color_name` / `color_hex` / `color_rgb` / `color_id`. Framed as the same instinct as *Do not use abbreviations*, taken one step further (an unabbreviated but vague name is still ambiguous), and cross-linked to it.

### Changed

- **PostgreSQL local-variable examples**: replaced the vague `__perms text[]` example with `__permission_full_codes text[]` in both the Casing summary table and the underscore-prefix rules table of `coding-guidelines-postgres/naming-conventions.md`, so the example demonstrates the new "say what the value holds" rule and matches the `permission.full_code` / `uq_permission_full_code` vocabulary.
- **Consolidated all schema/layering material onto one page**: the "Schema-shaped layering" and "public/internal/unsecure rule" sections in `coding-guidelines-postgres/index.md` duplicated the per-schema descriptions already in `schemas.md` (three times over, once the layer bullets, request-flow, and the "in one paragraph" rule were counted). Moved the request-flow diagram, the calling/security rules, and the worked two-wrapper/one-internal-worker example into `schemas.md`, and **merged** rather than pasted — per-schema detail now lives only in the schema table, the layer mapping is a compact three-line intro, and the security rule is a short "Calling rules" section (rules, not schema re-descriptions). `index.md` keeps a one-paragraph "Schema-shaped layering" pointer to `schemas.md`. Repointed the two inbound anchor links (`anti-patterns-and-example.md` → `schemas.md#calling-rules`; `coding-guidelines-elixir/index.md` → `schemas.md#schemas`).
- **Clarified the `helpers` / `internal` / `unsecure` split in PostgreSQL**: sharpened all three schema descriptions so the boundaries are explicit. `helpers.*` = **generic, domain-agnostic** pure functions that never touch tables (parse an order number from text, hash a jsonb, normalize a string). `internal.*` = the **general trusted implementation layer** for application domains — reads **and mutates** business tables with no permission check, reached either via a permission-checked `public.*` / `auth.*` wrapper (e.g. `public.get_documents_by_user_id` + `public.get_documents_by_username` both delegate to `internal.get_documents_by_user_id`) or from a trusted job with no user (`internal.recalculate_download_counts`); also home to resolvers/validators. `unsecure.*` = reserved **only** for highly sensitive auth/authz internals (permission-model mutations, permission-cache invalidation, token/session handling) — **not** for ordinary business mutations. Corrects the earlier "internal is read-only, mutations go in unsecure" framing. Updated the schema-table rows and the "`helpers` vs `internal`" note in `schemas.md`, the layering bullets + "don't cross the streams" warning + the one-paragraph rule (with a worked two-wrapper/one-internal-worker example) + the schema-shaped-layering sequence diagram (now shows the general `public.* → internal.* → table` path with the check via `auth.has_permission`, and a note that auth/authz-sensitive work uses an `auth.*` entry + `unsecure.*` worker) in `coding-guidelines-postgres/index.md`, the shared-logic anti-pattern remedy + a new "business mutation in `unsecure`" anti-pattern + a note on the worked example in `anti-patterns-and-example.md`, and the layering row in the "what's different" table.
- **Split the PostgreSQL naming conventions into focused pages**: the single 763-line `coding-guidelines-postgres/naming-conventions.md` was too long to hold attention, so it became eight flat sibling pages under `coding-guidelines-postgres/` — an overview `naming-conventions.md` (intro + master casing summary + See also) plus `schemas.md` (kept as a *structure* topic, not naming), `tables-columns-views.md`, `functions.md` (verbs, search shape, return types, stability, public-API boundary, qualification), `parameters-and-variables.md`, `triggers-indexes-constants-errors.md`, `migrations-and-files.md`, and `anti-patterns-and-example.md`. They sit directly under "PostgreSQL guidelines" in the nav (no extra nesting level to click through). Content is unchanged (byte-identical section slices); only relative links were re-based. Section headings — and therefore existing anchors — are preserved. Repointed all inbound links (C#, Elixir, general naming, `coding-guidelines-postgres/index.md`, and `claude/tech-stacks.json`): whole-file references target the overview, while the two anchored references land on their pages (the underscore-prefix rules → `parameters-and-variables.md`, the error-functions section → `triggers-indexes-constants-errors.md`).

## [2026-08-14]

### Added

- **C# / .NET coding guidelines**: filled in the previously-empty `coding-guidelines-csharp/index.md` and `naming-conventions.md`. Conventions distilled from the KeenMate DocumentHub backend reference implementation (`.NET 6`, ASP.NET Core, PostgreSQL, Serilog, Quartz), with Microsoft's C# conventions as the baseline and Bliss additions layered on top. `index.md` covers what's different from a typical Bliss service and what stays the same, the one-project-by-default rule (split into `*.Web` / `*.Database` / `*.Common` only when a second app reuses code or size demands it), the **layer-first vs feature-first** organization choice and when to use each (layer-first for console apps and moderate services; `Features/<Subject>/` when many similar CRUD-shaped features make "copy a feature folder and rename" the scaffolding move), the Impureim-sandwich mapping onto ASP.NET types (impure shell = controllers + providers like `DbContext` / `EmailProvider` / `ActiveDirectoryProvider`; pure core = mappers / helpers / models; the manager as the orchestration seam) with the "if it physically calls an external service, it is a Provider" warning, Controllers → Managers → Providers layering with a sequence diagram, the thin-controller rule, managers-orchestrate/providers-atomic, the autogenerated `db-gen` database layer (do-not-edit `Generated/*` and `*.generated.cs`), the `UserContext ctx`-first rule and correlation-id flow, Side-layer rules for Models / Mappers (static, pure) / Helpers (static, stateless) / Services (DI'd capabilities) with the Provider-vs-Service-vs-Manager distinction, Constants / Enums / Options, configuration (four-layer appsettings + env vars + user secrets, the Options pattern, DI lifetimes), the `ResponseModel<T>` / `ErrorResponseModel<T>` envelope, exception handling via `CatchMiddleware`, Serilog logging conventions, and Quartz background jobs under a system `UserContext`. `naming-conventions.md` covers the casing summary, projects/namespaces (mirroring folder path, incl. the feature-first namespace form), the `*Controller` / `*Manager` / `*Provider` / `*Service` suffixes, interface (`I*`) rules, Models / Mappers / Helpers / Options / Constants / Exceptions / Jobs naming, the C#-adapted verb registry with the mandatory `Async` suffix and the Get-vs-Search split, parameter order (`ctx` first, `CancellationToken cancellationToken` last), the deliberate house deviation of **plain-camelCase private fields** (no `_` prefix, assigned via `this.field = field`), constructor/DI conventions, logging, the registered upper-cased abbreviations (`AD` / `OID` / `GC` / `UTM` / `UUID` / `SHA`), deferral to the [PostgreSQL conventions](./coding-guidelines-postgres/naming-conventions.md) for everything generated from SQL functions, an anti-patterns table, and a worked controller → manager → provider example. Nav entries in `mkdocs.yml` already existed (the previous stubs were empty).

## [2026-06-29]

### Added

- **PostgreSQL search-function signature convention**: added a "Search functions — `_search_criteria` and `_search_settings`" subsection to `coding-guidelines-postgres/naming-conventions.md` (under Functions, after the `has_*`/`is_*` vs `check_*`/`validate_*` comparison). Every project-owned `search_*` function now takes its filters and presentation options as **two `jsonb` bags** instead of a positional list of `_filter`/`_page_size`/`_page_number`/`_order_by`: `_search_criteria` carries *what to find* (filters), `_search_settings` carries *how to return it* (paging, ordering, behavior toggles). Identity/audit params (`_user_id`, `_correlation_id`, `_tenant_id`) and `_display_language_code` (`text default 'en'`, only when the result carries translated labels) stay positional. Documents the rationale (signature stability — new filters/sorts are additive key-set changes, not signature churn, so callers/codegen/grants stay untouched; criteria-vs-settings is a real seam that changes for different reasons), the lenient-parsing rules (ignore unknown keys, default missing keys via guarded `coalesce((_search_settings->>'page')::int, 1)` extraction, whitelist `order_by`/`order_dir` before dynamic SQL, clamp `page_size`, default both bags to `'{}'::jsonb`), and notes that the per-function key-set comment becomes the contract since the signature no longer lists filters. Updated the `search_*` verb-registry row to the new shape, added `_search_criteria` / `_search_settings` / `_display_language_code` to the standard-parameter vocabulary, and added three anti-patterns (positional filter/paging params, interpolating `order_by` into dynamic SQL, raising on unknown criteria keys).

## [2026-06-24]

### Added

- **Impureim sandwich in Elixir**: added an "Impureim sandwich in Elixir" subsection to `coding-guidelines-elixir/index.md` (before "Layering, in Elixir terms"). The existing Elixir guidelines taught the layer *mechanics* (two-OTP-app split, controllers → contexts → providers, "providers don't talk to providers") but never named the *principle* behind them, so a mechanical reading led downstream work to inline external-service calls instead of isolating them. The new subsection cross-links the [Impureim Sandwich](../learning-guidelines/functional-vs-OOP-programming.md#impureim-sandwich-principal) explanation, maps the sandwich onto Elixir modules (impure shell = controllers + providers like `DbContext` / `GraphApi` / `Mailer` / `TwilioProvider`; pure core = mappers / `Helpers.*` / `Models.*`; the context as the impure-orchestration seam), and adds a warning admonition — "if it physically calls an external service, it is a Provider" — using the `TwilioProvider` example to make the rule concrete. A "What this buys you" subsection spells out the payoff: the Management layer becomes unit-testable in isolation via stub/fake providers (no Postgres / HTTP / job-queue spin-up), the same context path can be driven from any I/O shell (controller, Oban worker, `Mix` task, scheduled job) because the context is invocation-agnostic, and external calls live in exactly one place. Frames the next section's rules (thin controllers, one provider per external system, mappers with no I/O, providers-don't-call-providers) as consequences of the principle rather than arbitrary hygiene.

## [2026-06-21]

### Added

- **Elixir / Phoenix coding guidelines**: filled in the previously-empty `coding-guidelines-elixir/index.md` and `naming-conventions.md`. Conventions extracted from the DHL Location Factory backend reference implementation, with the Elixir-standards baseline only diverged from where Bliss adds a rule. `index.md` covers what's different from a typical Bliss service (BEAM runtime, two-OTP-app split, supervised stateful processes, `UserContext` threading, `:result` for unified errors), what stays the same (replaceability, ubiquitous language, DRY, restraint, Side-layer purity), the two-OTP-app split (`MyApp.*` business + `MyAppWeb.*` HTTP/Channels + `Database.*` autogenerated providers + `Models.*` / `Helpers.*` cross-cutting), Elixir-shaped layering with a sequence diagram (controllers → Phoenix contexts → `Database.DbContext` + external clients), the thin-controller rule, the context-orchestrates-mappers-shape-providers-do pattern, mechanical `DbContext` providers autogenerated by `db-gen`, the `UserContext` rule (built in `MyAppWeb.ConnHelpers.user_ctx/1`, always the first argument, `user_id: 1` reserved for the system actor, `request_id` flows end-to-end), Side-layer rules for `Models.*` / `Database.Models.*` (with the `@fields` → `@enforce_keys` → `defstruct` → `@derive Jason.Encoder` → `use Accessible` shape), `<Subject>Mapper` modules and `BaseMapper`, `Helpers.*` purity, `Consts` with `Memoize.defmemo` as the Elixir analogue of the PostgreSQL `const.*` schema, supervision-tree ordering and the one-PubSub-per-domain rule, and a four-tier configuration layering section covering `config/config.exs` → `config/<env>.exs` → optional `config/.local.exs` → optional `config/<env>.local.exs` with the `File.regular?(...) && import_config(...)` trick, the `.local.exs` vs `runtime.exs` split (per-developer overrides vs per-deploy reconfiguration), the project-wide `"FILL_ME_UP"` sentinel, the three env-var-reading shapes (`System.fetch_env!`, `System.get_env || raise """..."""`, `System.get_env("FOO", default)`), the "parse and validate at boot" rule, and the prod-guard on the `runtime.exs` block. The "Result type and error handling" section names the bestiary of bad shapes (`:ok` / `{:ok}` / `{:ok, nil}` / `{:ok, :no_data}` / `{:error, "string"}` / `{:error, %Ecto.Changeset{}}` / …), prescribes the [`:result`](https://hex.pm/packages/result) package as the Bliss default (`Result.ok/1` / `Result.error/1` / `Result.map/2` / `Result.and_then/2` / `Result.map_error/2` / `Result.with_default/2` / `Result.catch_error/3` / `Result.perform/2`), defines the operation-class shape rules for success payloads (`get_one` missing → `Result.error(:_not_found)` not `{:ok, nil}`; empty collections → `Result.ok([])`; paged → map inside `{:ok, _}` with `%{items, count, pages, page_size}`), atom-only error reasons with `{atom, map}` as the destructuring escape hatch, the interop-boundary rule (providers may return raw library tuples; contexts translate; controllers see only `Result.t()`), and a note that the earlier struct-based `Simplificator3000.Result` model is deprecated in favour of `:result` for new code. `naming-conventions.md` covers casing summary, top-level namespace split, context-and-subject filenames, controller / plug naming, struct shape rules, the Elixir verb registry adapted from the general Bliss registry (with `?` and `!` suffix rules), the two-arity "operate-on-self vs operate-on-other" pattern, the `get_my_*` prefix scoping rule, parameter conventions (`ctx` first, the `:eg_value_not_provided` sentinel allowed only in autogenerated `DbContext`, default-args style), standard parameter vocabulary (`ctx`, `params`, `filters`, `page`/`page_size`, `correlation_id`, `access_token`, `locale`), alias / import / use ordering, the Result naming side (constructors `Result.ok/1` / `Result.error/1` over raw tuple literals, combinators over `case` ladders, `Result.t(error, value)` as the spec type, verb→success-payload mapping), the project-wide logging shape via `Helpers.LoggingHelpers`, PubSub topic format `<concept>:<id>`, Behaviour / Adapter naming, file-header `@moduledoc` rule, deferral to the [PostgreSQL conventions](./coding-guidelines-postgres/naming-conventions.md) for everything generated from SQL functions, a comprehensive anti-patterns table (no `?` on boolean predicates, `do_*` / `_helper` decoration, per-context invented log shape, raw `{:ok, _}` literals as own-function output, adopting `Simplificator3000.Result` in new projects, `{:ok, nil}` for not-found, sentinel atoms in the success payload, mixing bare `:ok` and `{:ok, _}`, one-tuple `{:ok}`, leaking library structs as error reasons, string reasons, ad-hoc nested-tuple reasons, controllers matching library structs, hand-written `case` ladders where combinators fit, `raise` across a context boundary, `Repo.query` from a context, cross-context private-helper calls, current-user in process dictionary, `UserContext` built inside a context, bare `GenServer.start_link` outside the tree, one shared PubSub, hand-editing `Database.DbContext`, `MyApp.Helpers.LocationHelpers` for feature-specific code, `MyAppWeb` modules calling each other across feature boundaries, `@SCREAMING_SNAKE` attributes, action names not matching route verbs, committing `.local.exs`, hardcoding secrets in `prod.exs`, `runtime.exs` without the `prod` guard, silent `System.get_env` for required vars, compile-time `Application.get_env` via `@var`, ad-hoc sentinels instead of `"FILL_ME_UP"`), and a worked `MyApp.Locations.create/2` + matching `MyAppWeb.LocationController` example showing context, mapper, provider, controller, and fallback in one chain. Linked from the language-specific cards grid in `coding-guidelines/general-naming-conventions.md`; nav entries in `mkdocs.yml` already existed (the previous stubs were empty).

## [2026-06-20]

### Added

- **PostgreSQL coding guidelines**: filled in the previously-empty `coding-guidelines-postgres/index.md` and `naming-conventions.md`. Conventions extracted from the `postgresql-permissions-model` reference implementation and its `CLAUDE.md`. `index.md` covers what's different from a typical Bliss service, what stays the same, schema-shaped layering (`auth`/`public` as I/O, `unsecure` as Management, `internal`/`helpers`/`error`/`triggers`/`const` as Side), the public/internal/unsecure security rule, the multi-tenant access pattern (`_tenant_id` + `_target_tenant_id`, paired `read_*`/`read_all_*` permissions, `internal.resolve_cross_tenant_access`), forward-only version management via `public.__version` with components, and the audit/journal/`pg_notify` flow. `naming-conventions.md` covers the lowercase-SQL-keywords rule, the schema table, table/view singular-vs-plural, the mandatory `created_at`/`created_by`/`updated_at`/`updated_by` audit quartet, boolean prefixes (`is_`/`has_`/`allows_`/`should_`), the `nrm_` prefix for normalized search columns, the full PG verb registry (`get`, `search`, `create`, `update`, `delete`, `ensure`, `assign`/`revoke`, `enable`/`disable`, `lock`/`unlock`, `has`, `is`, `validate`/`verify`, `resolve`, `clear`/`invalidate`, `notify`, `calculate`, `raise_NNNNN`, `throw_*`), the underscore-prefix rules for parameters and variables (single `_` reserved for input parameters, double `__` for locals and `RETURNS TABLE(...)` columns, triple `___` only to disambiguate locals from `__`-prefixed return columns), standard parameter vocabulary (`_user_id`, `_target_user_id`, `_correlation_id`, `_throw_err`, `_request_context`, …), return-type shapes, stability/parallelism markers, the public-API-types boundary rule (only stock PG types + `jsonb` in `public.*`/`auth.*`; convert `text` ↔ `ext.ltree` internally), the always-fully-qualify-schema rule, trigger naming and the split by responsibility (`calculate_*`/`cache_*`/`notify_*`), index naming (`uq_`/`ix_`/`ix_trgm_`), the use-`const.*`-tables-instead-of-PG-enums rule, error functions (one per code, `error.raise_NNNNN(...)`, range table 30xxx–36xxx + 50xxx/52xxx app), migration file numbering conventions and the one-domain-per-file rule for the `020–028_functions_auth_*.sql` cluster, an anti-patterns table, and a worked `auth.assign_permission` + `unsecure.assign_permission` example showing the layering end-to-end.

## [2026-06-17]

### Added

- **Verb registry**: added `Check` to the standard verb list in `coding-guidelines/general-naming-conventions.md` and the `claude/naming-conventions.{md,txt}` mirrors. Introduced the _Check vs Validate vs Verify vs Is/Has/Can vs Ensure_ comparison table to settle when each verb applies (read-only inspection vs boundary-gate validation vs postcondition assertion vs property-style predicate vs idempotent upsert).
- **JavaScript / web-component coding guidelines**: new top-level section `coding-guidelines-javascript/` with `index.md` (component-shaped layering, what's different vs C#/PG, what stays the same) and `naming-conventions.md` covering casing, file naming, custom-element tag rules, class suffix conventions (`Element` for the custom-element wrapper), TS interface suffixes (`Config` / `Options` / `EventDetail` / `Context` / `Spec`), DOM-specific verbs (`render` / `handle` / `dispatch` / `attach` / `mount` / `observe` / `compute` / `position` / `commit` / `reconcile`), async vs sync conventions, boolean attribute semantics (`bool-default-true` vs `bool-default-false`), HTML attribute ↔ config-key mapping, the consumer-callback hierarchy (`on*` for Svelte-component notifications, `*Callback` for plain-JS config notifications, `before*Callback` for interceptors, `get*Callback` paired with `*Member` for data extractors, plain `*Callback` for behavior providers — with a decision flowchart and a quick test), `CustomEvent` naming (bare strings matching HTML standard), UI vocabulary collisions to watch for (`options` / `value` / `target` / `data` / `name` / `key` / `index`), and two worked examples — `@keenmate/web-multiselect` for the web-component side and `@keenmate/svelte-treeview` for the Svelte-component side, with a shared-conventions summary highlighting what's identical between them. Linked from the language-specific cards grid in `coding-guidelines/general-naming-conventions.md` and wired into `mkdocs.yml` nav.

### Fixed

- **CI/CD**: Repaired the `prod` deployment workflow (`.github/workflows/prod.yml`).
  - Removed broken `${{ inputs.dockerfile-name }}` reference (no `inputs` were defined on the job).
  - Upgraded action versions: `docker/login-action@v2` → `v3`, `docker/build-push-action@v5` → `v6`, `appleboy/ssh-action@v0.1.5` → `v1`.
  - Replaced deprecated `docker-compose` CLI with Docker Compose V2 (`docker compose`).
  - Split the single job into separate `build` and `deploy` jobs so SSH deploy only runs after a successful image push.
  - Dropped the unused `NPM_SCRIPT` build-arg (the Dockerfile is MkDocs-only) and the unnecessary `repository` / `token` parameters on `actions/checkout`.
